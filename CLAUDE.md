# CLAUDE.md

**Read `AGENTS.md` before doing anything in this repository.** It is the canonical agent guidance — dev commands, architecture, auth and tenancy rules, data model, import/export paths, and conventions. This file intentionally holds no copy of that content: one source of truth, one place to edit.

Beyond `AGENTS.md`:

- `CONTEXT.md` — domain language and entity semantics. Read it before touching anything that uses domain vocabulary.
- `docs/adr/` — architectural decisions and their reasons.
- `docs/agents/` — issue tracker, triage labels, domain-doc conventions.

When you learn something durable about this codebase, add it to `AGENTS.md`, not here.
